import SwiftUI

/// Placeholder; app now uses SplashView → MainTabView from ConnectApp.
struct ContentView: View {
    var body: some View {
        MainTabView()
    }
}

#Preview {
    ContentView()
}
